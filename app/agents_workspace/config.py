from __future__ import annotations

WORKSPACE_AGENT_NAME = "workspace_agent"
WORKSPACE_AGENT_DESCRIPTION = (
    "Manages workspaces and linked GitHub/GitLab repositories. "
    "Can list remote repos, clone them locally, and coordinate with the git agent."
)
